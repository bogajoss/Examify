# Examify Environment Variables Template
# Copy this to .env.local and fill in your actual values

# ====================
# SUPABASE CONFIGURATION
# ====================

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous/Public Key (safe to expose to frontend)
# Use for client-side operations only (limited by RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Publishable Key (optional)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_...

# Supabase Service Role Key (KEEP SECRET - server-side only!)
# This key has full access to all tables and should NEVER be exposed to the client
# Only use this in server actions, API routes, and migrations
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ====================
# OPTIONAL: CSV BACKEND API
# ====================
# Only needed if using legacy PHP backend for CSV operations
# Otherwise, use the new Supabase CSV upload endpoints

# Backend API Base URL
NEXT_PUBLIC_CSV_API_BASE_URL=https://your-api.example.com

# Backend API Key
NEXT_PUBLIC_CSV_API_KEY=your-api-key-here

# ====================
# INSTRUCTIONS
# ====================
# 
# 1. Go to your Supabase project dashboard
# 2. Click "Settings" in the bottom left
# 3. Click "API" tab
# 4. Find your credentials:
#    - Project URL: Copy the "Project URL" field
#    - ANON KEY: Copy from "anon | public"
#    - SERVICE ROLE KEY: Copy from "service_role | secret"
# 5. Paste them here, replacing the placeholder values
#
# WARNING: Never commit this file to git if it contains real keys!
# Add .env.local to .gitignore
#
# LOCAL DEVELOPMENT:
# - Use the credentials from your development Supabase project
#
# PRODUCTION:
# - Use the credentials from your production Supabase project
# - Deploy .env.local via your hosting platform's environment variable settings
#   (e.g., Vercel, Netlify, AWS, etc.)
